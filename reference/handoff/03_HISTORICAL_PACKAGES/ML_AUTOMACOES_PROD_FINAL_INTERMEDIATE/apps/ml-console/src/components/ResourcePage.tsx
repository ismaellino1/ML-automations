import {useState} from "react";
export function ResourcePage({title,description,children}:{title:string,description?:string,children?:React.ReactNode}){const[open,setOpen]=useState(false);return <section><header className="pageHead"><div><h1>{title}</h1><p>{description}</p></div><button onClick={()=>setOpen(!open)}>{open?"Fechar":"Nova ação"}</button></header>{open&&<div className="panel">As ações desta tela são executadas pelo Control API com RBAC e auditoria.</div>}{children??<div className="panel">Dados carregados conforme a empresa e as permissões do usuário.</div>}</section>}
