
-- Run in Supabase test environment:
-- 1. authenticated user sees only own memberships;
-- 2. non-member cannot execute business control actions;
-- 3. PLATFORM_ADMIN can act cross-tenant only through audited control plane.
